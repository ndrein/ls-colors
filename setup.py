from setuptools import setup

setup(name='ls-colors',
      version='0.1',
      description='Shell utility for colouring ls',
      author='Noah Reinhardt',
      author_email='ndrein@gmail.com',
      scripts=['bin/ls'])